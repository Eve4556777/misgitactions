name: Github Actions Practice

on: [push]

jobs:
  Explorando-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar información del evento
        run: echo "El flujo de trabajo ha sido ejecutado en el evento: ${{ github.event_name }}"
      
      - name: Mostrar información del runner
        run: echo "El trabajo se está ejecutado en un servidor ${{ runner.os }} hospedado en Github"
      
      - name: Verificar el código del repositorio
        uses: actions/checkout@v3
      
      - name: Listar archivos
        run: ls -la
